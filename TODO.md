# TODO

## Назначение файла

Этот файл - рабочий backlog проекта.

Сюда выносятся:

- продуктовые задачи
- фиксы и техдолг
- синхронизация с похожими ботами
- исследовательские задачи по зависимостям и нестабильным местам

## High Priority

- [x] Перейти с `telegraf` на `telegraf-hardened` и NestJS-адаптер
      `nestjs-telega`.
      Статус: кодовая миграция на `nestjs-telega@1.0.0-rc.2` и
      `telegraf-hardened@6.0.0` завершена, включая middleware, scenes,
      exception filters, Redis sessions, fallback-listener, typed
      `sendMessageDraft`, `ctx.react()` и `polling.retryOnConflict`.
      Автоматические проверки пройдены. Ручные проверки на тестовом боте перед
      production остаются в `docs/agent-wiki/TODO-temp.md`.

- [x] Проверить и исправить падение/некорректное поведение при Telegram `403: bot was kicked from the group chat`.
      Статус: `TelegrafExceptionFilter` распознаёт недоступную беседу,
      отмечает `Conversation.isLeaved` и завершает обработку без повторного
      ответа в недоступный чат.

- [x] Проверить и стабилизировать обработку Telegram `429 Too Many Requests`.
      Статус: transport filter распознаёт rate limit и не пытается отправить
      пользователю ещё одно сообщение; streaming draft повторяется с
      `retry_after`.

- [x] Стабилизировать обработку блокировок при всплеске апдейтов.
      Статус: старый `redlock` заменён на `ConcurrencyService` с локальными
      keyed mutex/queue и контролируемой distributed-блокировкой; конфликты
      преобразуются в обрабатываемую ошибку конкурентного доступа.

- [x] Подготовить базовый модуль управляемой рассылки для Telegram/VK.
      Текущее состояние: добавлен `BroadcastModule` с Bull-очередью, Telegram wizard, простым VK text-flow, PG-сущностями кампаний/доставок и настраиваемым лимитом истории.

- [x] Добавить пользователям ежедневную рассылку расписания.
      Статус: `ScheduleNotifModule` хранит настройки и историю доставок,
      scheduler запускается каждую минуту по московскому времени, а Telegram
      и VK предоставляют настройку, изменение, отключение и удаление рассылки.
      Зарезервированные доставки выполняются через отдельные Bull-очереди
      Telegram/VK с retry/backoff для временных ошибок, восстановлением
      недавних pending-задач и отсечением устаревших сообщений.

- [x] Подготовить конкретную рассылку или уведомление для пользователей Telegram о восстановлении работы бота.
      Статус: выполнено.

- [x] Расширить фильтры аудитории broadcast и их UI в Telegram/VK.
      Статус: реализованы `onlyAuthorized`, выбор нескольких нормализованных
      групп, исключение получателей уже выбранных кампаний, активность до/с
      даты и в диапазоне с опцией включить профили без активности, а также
      повторная выборка Telegram-доставок, не дошедших из-за rate limit.
      В Telegram/VK есть UI, preview и сброс ручного списка получателей при
      смене фильтра. `profileType` и сравнение курса группы остаются отдельными
      задачами, так как для них пока нет подтверждённой модели данных.

- [ ] Сделать информирование о значимых изменениях в расписании выбранной группы.
      Идея: кэшировать расписание на день/неделю, периодически сверять с Schedule API и отправлять уведомление, если пары изменились. Детали дедупликации, окна сравнения и формат diff продумать отдельной задачей.
      Целевой вариант: добавить в Schedule API отдельный контракт подписки или
      события об обновлении расписания, чтобы бот получал факт изменения, а не
      постоянно сравнивал весь кэш по таймеру. После этого спроектировать
      opt-in подписки для групп/преподавателей, дедупликацию, окно сравнения,
      формат diff и доставку в Telegram/VK. Polling-сверка кэша допустима лишь
      как отдельно согласованный fallback, если API-событие недоступно.

- [x] Доработать метрики до подробной статистики и стабильного восстановления после перезагрузки сервиса.
      Статус: domain gauges восстанавливаются из БД при bootstrap и каждую
      минуту; добавлены breakdown пользователей, соцпрофилей и бесед, метрики
      feedback, отключённых уведомлений, справочника и наполненности
      расписания. Counters покрывают запросы расписания, создание уведомлений
      и входящие Telegram/VK updates. Полный контракт и PromQL-примеры:
      `docs/monitoring/metrics-reference.md`.

- [ ] Как в проекте social-connect переработать логику работы с `redlock`.

## Medium Priority

- [ ] Добавить различение типа профиля: студент / сотрудник / преподаватель.
      Ожидаемый результат: профиль и связанные сообщения отображаются в корректном формате для типа пользователя.

- [ ] Для студента показывать его группу в профиле.
      Ожидаемый результат: профиль студента отражает актуальную учебную группу.

- [x] Добавить явный выбор и просмотр расписания преподавателя.
      Статус: Telegram и VK поддерживают поиск по `/teacher Фамилия`, постраничный список `/tlist`, сохранение выбранного преподавателя в messenger-session и команды `/tday` / `/tweek`.
      Дополнительно: для привязанного профиля используется только однозначное совпадение ФИО с актуальным списком преподавателей как fallback.

- [ ] Проверить, совместим ли `teacherId` при авторизации с ID расписания и при подтверждении добавить авторитетную постоянную привязку преподавателя к профилю.
      Контекст: текущий OAuth-ответ не содержит `teacherId`; fallback по ФИО не является подтвержденной связью.

- [x] Проверить кейс `VK` с пустым `ctx.peerId` на событиях типа `message_subscription`.
      Статус: feature middleware подставляет `userId` как `peerId` до
      пользовательской session и обработки профиля; `message_allow` и
      `message_deny` обновляют доступность ЛС без ответа пользователю.

## Low Priority

- [x] Переименовать модуль ystuty в `schedule` для большей ясности.
      Статус: `ScheduleModule` и `ScheduleService` объединяют работу с
      расписанием, группами и преподавателями; уведомления остаются отдельным
      модулем `schedule-notif`.

- [ ] Проработать сценарий расписания на месяц.
      Идея: выбор недель внутри месяца с навигацией по диапазонам дат.

- [x] Добавить отображение недель в формате диапазонов дат.
      Статус: заголовок недельного расписания показывает диапазон
      понедельник–воскресенье и номер недели, например `[1] … (1–7 сентября)`.

- [x] Сделать смещение выбора следующей недели по логике, похожей на текущую пагинацию.
      Статус: Telegram и VK показывают inline-переходы на предыдущую и
      следующую недели только внутри доступного диапазона Schedule API; номер
      недели передаётся callback-параметром, а не хранится в session.

- [ ] Добавить кнопки выбора дня недели для месячного сценария.
      Пример: `Пн`, `Вт`, `Ср`, `Чт`, `Пт`, `Сб`, `Вс`.

- [ ] Для недоступных дней предусмотреть визуально неактивное состояние.
      Идея: зачеркнутые или иным образом помеченные дни.

## Tech Debt

- [x] Актуализировать `README.md`, чтобы он лучше отражал текущее устройство проекта.
      Статус: удалён шаблон NestJS, добавлены описание бота, возможности,
      ссылки на транспорты и команды локального запуска/проверок.

- [ ] Подготовить безопасный production-переезд со старой версии приложения на текущую.

- [x] Перевести production compose на готовый Docker image вместо сборки на сервере.
      Статус: `docker-compose.deploy.yml` использует GHCR image, а Makefile
      поддерживает pull/deploy и SHA-тег образа для rollback.

- [ ] Нормально настроить инфраструктуру для unit/spec тестов.
      Контекст: сейчас проект не подготовлен для удобного запуска локальных `*.spec.ts` с корректной типизацией и предсказуемым test environment.
      Ожидаемый результат: можно добавлять точечные regression/unit tests без лишней ручной настройки под каждый файл.

- [ ] Проверить, стоит ли частично выделить общие части ботов в более формализованный общий слой или пакет.
      Контекст: сейчас синхронизация между проектами делается вручную и кусками.

- [ ] Пересмотреть обработку ошибок мессенджерных API в filters и service-слое.
      Ожидаемый результат: меньше необработанных edge-case ошибок и меньше риска падения процесса.

- [ ] Подготовить transport helper для отправки стартовых/служебных сообщений через разные мессенджеры.
      Контекст: аналогично идее `sendMessageStart`, но адаптировать под текущий проект и хранить в transport/state-слое, чтобы не размазывать messenger-specific отправку по update-классам.

- [ ] Спроектировать следующий уровень keyboard builder для Telegram и VK.
      Контекст: оценить, должен ли builder привязываться к `ctx` и предоставлять
      базовые transport-возможности, либо каждый доменный модуль должен иметь
      свой builder поверх общих helpers. Не вводить общий registry клавиатур без
      подтверждённой потребности: он должен упрощать composition, i18n и
      transport-ограничения, а не быть только каталогом методов factory.

- [ ] Добавить i18n-renderer для фоновых broadcast progress-сообщений.
      Контекст: Bull processor не имеет `ctx`, поэтому сейчас progress text формируется fallback-строкой без пользовательского locale context.

- [ ] Проверить, нужен ли отдельный throttling / retry policy для тяжелых ответов Telegram.

- [ ] Пересмотреть polling-подход в `SocialConnectService.checkAuth()`.
      Идея из черновиков: возможно перейти к более аккуратной модели ожидания или long-poll механике.

## Sync With Sibling Projects

- [ ] При следующей синхронизации сравнить guards, filters и middleware с `social-connect` и другими похожими ботами.

- [ ] Подготовить перенос модуля для мессенджера `max`, когда его архитектура в sibling-проекте стабилизируется.
      Важно: переносить не только transport layer, но и связанные типы, middleware и service-контракты.

- [ ] Регулярно сверять `UserService`, `TelegramService`, `VkService` и `SocialConnectService` с аналогичными сервисами в соседних проектах.

- [ ] Проверять не только diff по коду, но и совместимость env-конфига, package versions и session key conventions.

## Legacy Notes

- [ ] ...
