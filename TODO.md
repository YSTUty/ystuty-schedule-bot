# TODO

## Назначение файла

Этот файл - рабочий backlog проекта.

Сюда выносятся:

- продуктовые задачи
- фиксы и техдолг
- синхронизация с похожими ботами
- исследовательские задачи по зависимостям и нестабильным местам

## High Priority

- [ ] Проверить и исправить падение/некорректное поведение при Telegram `403: bot was kicked from the group chat`.
      Ожидаемый результат: бот не падает, кейс корректно логируется и не ломает дальнейшую обработку.

- [ ] Проверить и стабилизировать обработку Telegram `429 Too Many Requests`.
      Ожидаемый результат: при rate limit сервис не валится, а деградирует контролируемо.

- [ ] Проверить работу `redlock` после потери соединения и при всплеске апдейтов.
      Ожидаемый результат: ошибки блокировок не приводят к крашу процесса и не создают каскадных сбоев.

- [ ] Подготовить рассылку или уведомление для пользователей Telegram о восстановлении работы бота.
      Контекст: можно дополнительно напомнить про наличие бота во `VK`.

- [ ] Как в проекте social-connect переработать логику работы с `redlock`.

## Medium Priority

- [ ] Добавить различение типа профиля: студент / сотрудник / преподаватель.
      Ожидаемый результат: профиль и связанные сообщения отображаются в корректном формате для типа пользователя.

- [ ] Для студента показывать его группу в профиле.
      Ожидаемый результат: профиль студента отражает актуальную учебную группу.

- [ ] Для преподавателя показывать его ID или отдельную кнопку выбора расписания преподавателя.
      Ожидаемый результат: сценарий выбора расписания преподавателя становится явным и понятным.

- [ ] Проверить - получение id teacher при авторизации совместим ли с ID расписания для него.

- [ ] Проверить кейс `VK` с пустым `ctx.peerId` на событиях типа `message_subscription`.
      Ожидаемый результат: такие события обрабатываются безопасно и не ломают middleware/update pipeline.

## Low Priority

- [ ] Переименовать модуль ystuty в `schedule` или `schedule-service` для большей ясности.
      Контекст: название `ystuty` не сразу ассоциируется с функционалом расписания.

- [ ] Проработать сценарий расписания на месяц.
      Идея: выбор недель внутри месяца с навигацией по диапазонам дат.

- [ ] Добавить отображение недель в формате диапазонов дат.
      Пример: `05.02-11.02`, `12.02-18.02`.

- [ ] Сделать смещение выбора следующей недели по логике, похожей на текущую пагинацию.

- [ ] Добавить кнопки выбора дня недели для месячного сценария.
      Пример: `Пн`, `Вт`, `Ср`, `Чт`, `Пт`, `Сб`, `Вс`.

- [ ] Для недоступных дней предусмотреть визуально неактивное состояние.
      Идея: зачеркнутые или иным образом помеченные дни.

## Tech Debt

- [ ] Актуализировать `README.md`, чтобы он лучше отражал текущее устройство проекта.

- [ ] Нормально настроить инфраструктуру для unit/spec тестов.
      Контекст: сейчас проект не подготовлен для удобного запуска локальных `*.spec.ts` с корректной типизацией и предсказуемым test environment.
      Ожидаемый результат: можно добавлять точечные regression/unit tests без лишней ручной настройки под каждый файл.

- [ ] Проверить, стоит ли частично выделить общие части ботов в более формализованный общий слой или пакет.
      Контекст: сейчас синхронизация между проектами делается вручную и кусками.

- [ ] Пересмотреть обработку ошибок мессенджерных API в filters и service-слое.
      Ожидаемый результат: меньше необработанных edge-case ошибок и меньше риска падения процесса.

- [ ] Проверить, нужен ли отдельный throttling / retry policy для тяжелых ответов Telegram.

- [ ] Пересмотреть polling-подход в `SocialConnectService.checkAuth()`.
      Идея из черновиков: возможно перейти к более аккуратной модели ожидания или long-poll механике.

## Sync With Sibling Projects

- [ ] При следующей синхронизации сравнить guards, filters и middleware с `social-connect` и другими похожими ботами.

- [ ] Подготовить перенос модуля для мессенджера `max`, когда его архитектура в sibling-проекте стабилизируется.
      Важно: переносить не только transport layer, но и связанные типы, middleware и service-контракты.

- [ ] Регулярно сверять `UserService`, `TelegramService`, `VkService` и `SocialConnectService` с аналогичными сервисами в соседних проектах.

- [ ] Проверять не только diff по коду, но и совместимость env-конфига, package versions и session key conventions.

## Research / Dependencies

- [ ] Вернуться к обновлению `@nestjs/typeorm` после ревизии версий `typeorm` и `pg`.
      Текущее состояние: пакет временно закреплен на `@nestjs/typeorm@11.0.0`.
      Причина: на `11.0.1` наблюдались `undefined` imports и проблемы с чтением metadata.
      Ожидаемый результат: понять точную причину несовместимости и безопасно обновиться.

- [ ] При обновлении зависимостей отдельно проверить bootstrap приложения, чтение metadata и загрузку entity-моделей.

- [ ] Собрать минимальный воспроизводимый сценарий для проблемы с `@nestjs/typeorm@11.0.1`, если баг повторится.

## Legacy Notes

- [ ] ...
